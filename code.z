include("std");

func sayHi() { print("hi"); }

var a = 1;

var version = 99;

func loop(a, b) {
  while (b > 0) {
    print(a * 1);
    b = b - 1;
  }
}

func action(name, num) {
  while (num > 0) {
    print(name);
    num = num - 1;
  }
}

// This is main Function

/*
 This is main Function
*/
func main() {


  var human = {"run" : action};


  var all = {"owner" : human};

  all.owner.run("owner running", 3);
}