
func main() {
  var s = "hello";
  native_println(s);
  native_println(s.len());
  native_println(s.substr(0, 3));
  native_println(s.equal("hello"));
  native_println(s.equal("hel"));
  s.concat(" world!");
  native_println(s);
  var x = "";
  native_println(x.len());
}