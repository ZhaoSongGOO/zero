include("std");

func action(pair){
    print(pair.key);
    print(pair.value);
}

func main(){
    var m = Map();
    m.insert(Int(1), Int(10));
    print(m);
    m.insert(Int(2), Int(20));
    print(m);
    m.insert(Int(1), Int(30));
    print(m);
    m.each(action);
}