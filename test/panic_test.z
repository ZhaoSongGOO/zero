include("std-library/lite/v1/lib/std-lite-v1");
func main() {
  assert(true, "It's a panic form zero assert api with condition `true`");
  assert(null, "It's a panic form zero assert api with condition `null`");
  assert(1 == 2, "It's a panic form zero assert api with condition `1==2`");
  assert(1 < 2, "It's a panic form zero assert api with condition `1<2`");
  panic(1, "It's a panic form zero script!");
}